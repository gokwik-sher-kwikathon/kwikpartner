import React from 'react';
import IncentiveTracker from '../../components/service/IncentiveTracker';

const IncentiveTrackerPage: React.FC = () => {
  return <IncentiveTracker />;
};

export default IncentiveTrackerPage;
