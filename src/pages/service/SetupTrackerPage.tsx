import React from 'react';
import SetupTracker from '../../components/service/SetupTracker';

const SetupTrackerPage: React.FC = () => {
  return <SetupTracker />;
};

export default SetupTrackerPage;
