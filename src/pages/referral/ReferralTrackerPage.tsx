import React from 'react';
import ReferralTracker from '../../components/referral/ReferralTracker';

const ReferralTrackerPage: React.FC = () => {
  return <ReferralTracker />;
};

export default ReferralTrackerPage;
