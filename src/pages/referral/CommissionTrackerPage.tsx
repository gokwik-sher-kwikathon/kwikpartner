import React from 'react';
import CommissionTracker from '../../components/referral/CommissionTracker';

const CommissionTrackerPage: React.FC = () => {
  return <CommissionTracker />;
};

export default CommissionTrackerPage;
